n = int(input())
count = 0
for i in range(1, n+1):
    if i < 100:
        count += 1
    else:
        s = str(i)
        a = int(s[0])
        b = int(s[1])
        c = int(s[2])

        if b - a == c - b:
            count += 1

print(count)
